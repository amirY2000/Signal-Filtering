function y = medianFunction(signal, windowSize)    
        n = 0:size(signal);
        for k = 0 : windowSize
                   tempArray = signal(n-k)
        end
        y(n) = 
        
        
end